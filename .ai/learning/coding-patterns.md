# Project Coding Patterns

Capture approved recurring patterns for API responses, validation, error handling,
React state, authentication, tests, prompts, and persistence.

For every pattern include: problem, approved solution, example location, constraints,
and anti-patterns. Do not copy secrets or real candidate data.
