# Common Bugs

Record recurring defects with: symptom, root cause, affected area, detection method,
fix, regression test, and prevention rule.

Focus on auth failures, file uploads, PDF/DOCX parsing, provider failures, prompt JSON
validation, FAISS persistence, and frontend/API contract mismatches.
