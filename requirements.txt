# --- Web framework / API server ---
# Bumped to latest 0.x so the resolver can pick a patched starlette.
# The 0.124 line capped starlette<0.51.0, leaving only the vulnerable
# 0.50.0 available. Do not pin starlette here.
fastapi==0.139.2
uvicorn==0.38.0
python-multipart==0.0.31
pydantic==2.9.2

# --- Data + ML core ---
# transformers 5.x closes the remaining CVEs that 4.53.0 left open.
# sentence-transformers 5.x is the matching companion (its 4.x line
# caps transformers<5.0.0).
pandas==2.2.3
numpy==2.2.6
scikit-learn==1.6.1
faiss-cpu==1.13.1
sentence-transformers==5.1.0
torch==2.13.0
transformers==5.3.0
# transformers 5.x requires huggingface-hub>=1.3.0; 0.30.0 would conflict.
huggingface-hub==0.34.0

# --- LLM providers ---
ollama==0.5.1
google-genai==2.8.0

# --- File parsing ---
pypdf==6.13.3
python-docx==1.1.2
reportlab==4.2.5

# --- Validation / config ---
jsonschema==4.24.0
PyYAML==6.0.2
python-dotenv==1.2.2

# --- HTTP clients ---
httpx==0.28.1
requests==2.33.0

# --- Testing & profiling ---
# pytest 9.0.3 closes PYSEC-2026-1845. pytest-playwright 0.7.0 removed
# the pytest<9.0.0 cap that 0.5.2 had, so the 9.x line is now usable.
pytest==9.0.3
pytest-mock==3.14.0
pytest-benchmark==4.0.0
pytest-playwright==0.7.0
playwright==1.49.1
allure-pytest==2.16.0
scalene==1.5.33

# --- Security / quality scans ---
bandit==1.7.10
safety==3.2.3
lighthouse==1.0.0
axe-playwright-python==0.1.4